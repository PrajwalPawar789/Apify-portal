<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Excel File</title>
    <!-- Link to Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom neon glow effect */
        .neon-text {
            text-shadow: 0 0 10px rgba(255, 255, 255, 0.8), 0 0 30px #4caf50, 0 0 50px #4caf50, 0 0 100px #4caf50;
        }

        .neon-btn:hover {
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.8), 0 0 20px #4caf50, 0 0 40px #4caf50;
        }

        .neon-input:focus {
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.8), 0 0 30px #4caf50;
        }

        /* Glow effect for navbar items */
        .neon-navbar:hover {
            color: #4caf50;
            text-shadow: 0 0 10px #4caf50, 0 0 20px #4caf50;
        }
    </style>
</head>
<body class="bg-gray-900 text-white font-sans">

    <!-- Navbar -->
    <nav class="bg-black bg-opacity-80 p-5 shadow-lg fixed w-full top-0 left-0 z-50">
        <div class="container mx-auto flex justify-between items-center">
            <a href="#" class="text-2xl font-semibold text-transparent bg-clip-text bg-gradient-to-r from-green-400 via-blue-500 to-purple-600 neon-text hover:text-white">Manlitics Linkedin Scrapper.</a>
            <div class="space-x-6 text-lg text-gray-300">
                <a href="#" class="neon-navbar hover:text-white">Home</a>
                <a href="#" class="neon-navbar hover:text-white">Features</a>
                <a href="#" class="neon-navbar hover:text-white">Contact</a>
            </div>
        </div>
    </nav>

    <!-- Main content -->
<div class="flex justify-center items-center min-h-screen bg-gradient-to-r from-gray-900 via-gray-800 to-gray-900 p-6">
    <div class="bg-gray-800 bg-opacity-90 p-8 rounded-3xl shadow-2xl max-w-lg w-full transform transition-all hover:scale-105 hover:shadow-3xl">
        <h1 class="text-4xl font-semibold text-center text-transparent bg-clip-text bg-gradient-to-r from-green-400 via-blue-500 to-purple-600 mb-6 neon-text">
            Upload Your Excel File
        </h1>
        <form action="/process-file" method="POST" enctype="multipart/form-data" class="space-y-6">

            <!-- File Input -->
            <div>
                <label for="excelFile" class="block text-lg text-gray-300">Select Excel File</label>
                <input 
                    type="file" 
                    name="excelFile" 
                    accept=".xlsx" 
                    required 
                    class="mt-2 w-full px-4 py-3 border-2 border-gray-700 rounded-lg bg-gray-900 text-white placeholder-gray-500 focus:outline-none focus:ring-4 focus:ring-green-500 focus:border-green-500 transition duration-300"
                >
            </div>

            <!-- Submit Button -->
            <button 
                type="submit" 
                class="w-full bg-gradient-to-r from-green-500 to-blue-500 text-white font-semibold py-3 rounded-lg transform transition-all hover:scale-105 hover:bg-gradient-to-r hover:from-green-600 hover:to-blue-600 hover:shadow-xl"
            >
                Upload and Process
            </button>
        </form>
    </div>
</div>


    <!-- Footer (optional) -->
    <footer class="bg-black bg-opacity-80 py-4 text-center text-gray-400">
        <p>&copy; 2024 Manilitics Linkedin Scrapper. All rights reserved.</p>
    </footer>

</body>
</html>
